package Utils;

public class Helper {
    public static boolean compare(int key, int average)
    {
        if (key >= average) {
            return true;
        } else {
            return false;
        }
    }
}
